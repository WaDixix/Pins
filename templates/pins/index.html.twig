{% extends 'base.html.twig' %}

{% block title 'Home' %}

{% block body %}
    <h1>Dsoler frere</h1>
    {% for pin in pins %}
    <div class="card mt-3">
        <div class="card-header">
            <h4>{{ pin.title }}</h4>
        </div>
        <div class="card-body">
            <p>{{ pin.description }}</p>
            <a href="{{ path('show', {'id': pin.id}) }}" class="btn btn-primary">Voir</a>
        </div>
    </div>
    {% else %}
    <p>Désoler il y a rien ici!</p>
    {% endfor %}
{% endblock %}